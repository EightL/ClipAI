# ClipAI Popup (Minimal)

Global hotkey popup using Electron.

Hotkey: macOS `Cmd+Shift+Space`, Windows/Linux `Ctrl+Shift+Space`.

## Dev
```
npm install
npm run dev
```

## Build installers (mac/win/linux)
```
npm run build
```

Output will appear in `dist/`.
